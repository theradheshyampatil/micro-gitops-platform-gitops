apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: product-service
  namespace: apps
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: traefik
  rules:
    - host: product.projectbyradhe.xyz
      http:
        paths:
          - path: /products
            pathType: Prefix
            backend:
              service:
                name: product-service
                port:
                  number: 80
          - path: /health
            pathType: Prefix
            backend:
              service:
                name: product-service
                port:
                  number: 80
