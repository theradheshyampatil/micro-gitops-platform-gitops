apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: user-service
  namespace: apps
spec:
  rules:
    - host: user.13.201.174.4.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: user-service
                port:
                  number: 80
